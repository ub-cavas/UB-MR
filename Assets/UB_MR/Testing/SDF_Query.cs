using UnityEngine;

public class SDF_Query : MonoBehaviour
{
    public SDFTexture sdfTexture;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //sdfTexture.sdf
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
